apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.backend.name }}
  labels:
    {{- include "app.labels" (dict "name" .Values.services.backend.name "Values" .Values "Release" .Release) | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    {{- range .Values.services.backend.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: http
    {{- end }}
  selector:
    app: {{ .Values.deployments.backend.name }}
