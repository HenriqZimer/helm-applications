apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.frontend.name }}
  labels:
    {{- include "app.labels" (dict "name" .Values.services.frontend.name "Values" .Values "Release" .Release) | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    {{- range .Values.services.frontend.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: http
    {{- end }}
  selector:
    app: {{ .Values.deployments.frontend.name }}
